要考虑的问题：
1. 实时数据处理
   1. 采样率与时间跨度
   这里暂时设置电压采样率为1000hz，采样的时间跨度为10ms，即每次采样会获取10个数据，`number_of_samples_per_channel=10`，即采集的原始电压数据形状为[6,10]
   2. 矩阵计算时间
   遵循的步骤是 collect Voltage ---> calibration matrix ----> get force/torque
   3. 后续处理的实时性
   因为下位机跟pc的通讯时间是10ms，采取10ms读取一次传感器力的数据计算位置，==这个需要后续考虑具体多久一次==，以及应该读取什么数据呢？
      - **目前采用**单点数据，读取10个样本中的最后一个数据，低延迟但噪声敏感
      - 窗口内平均值，抗噪声但有10ms的平均延迟
      - 实时滤波，低通滤波平均延迟和噪声
    